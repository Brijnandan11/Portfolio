# Deploy Checklist

1. Log in to Vercel with the `Brijnandan11` account.
2. Import the GitHub repo `Brijnandan11/Portfolio`.
3. Use the `main` branch for Production.
4. Keep the build command as `npm run build`.
5. Keep the output directory as `dist`.
6. Deploy.

Notes:
- The site is code-first and does not depend on the old seeded API content.
- The favicon is already configured to use the `B` icon.
- If Vercel asks about linking, choose the `Brijnandan11` GitHub repo, not the old `edtcodes-projects` one.
