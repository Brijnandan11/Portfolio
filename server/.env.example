# Neon Postgres connection string — https://neon.tech → your project → Connection Details
DATABASE_URL=postgres://USER:PASSWORD@HOST/DBNAME?sslmode=require

# Admin panel password — pick a long random string (this is your login)
ADMIN_TOKEN=change-me-to-something-long-and-random

# Optional, defaults to 8787
PORT=8787
